Purpose. It calculates how many parameters (Billions) you can run based on your memory, accounting for context overhead and different quantization levels (4-bit, 8-bit, FP16). 

For a 24GB GPU with INT4 quantization, you'll now see:

Max Model on GPU
70 B        ← Parameter count
36.48 GB    ← Actual file size
